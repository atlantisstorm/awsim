import React from 'react';
import loadingGraphic from "../graphics/loading.gif";
const Loading = () => {
  return (
    <div id="loading">
      <img src={loadingGraphic} />
    </div>
  )
}

export default Loading;